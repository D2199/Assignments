#HTML
Here are the 20 multiple-choice questions on HTML with their respective answers: 

1. What does HTML stand for? 
   a) **Hypertext Markup Language** 
   b) Hyperlinked Text Markup Language 
   c) Hypertext Markup Layout 
   d) Hyperlinked Markup Language 

2. Which of the following is the correct HTML tag to insert an image? 
   a) **<img>** 
   b) <image> 
   c) <picture> 
   d) <img src> 

3. What is the purpose of the `<head>` element in HTML? 
   a) To define the document's header 
   b) **To specify the document's metadata** 
   c) To contain the main content of the page 
   d) To style the document's appearance 

4. How do you create a hyperlink in HTML? 
   a) **<a href="url">Link Text</a>** 
   b) <link url="url">Link Text</link> 
   c) <hyperlink url="url">Link Text</hyperlink> 
   d) <url>Link Text</url> 

5. Which HTML tag is used to create a division or section in a document? 
   a) **<div>** 
   b) <section> 
   c) <group> 
   d) <container> 

6. What does the `alt` attribute do in an `<img>` tag? 
   a) **Provides alternative text for screen readers** 
   b) Specifies the image's height 
   c) Defines the image's source URL 
   d) Sets the image's width 

7. How can you add a comment in HTML? 
   a) **<!-- This is a comment -->** 
   b) /* This is a comment */ 
   c) // This is a comment 
   d) # This is a comment 

8. Which tag is used to create a paragraph in HTML? 
   a) **<p>** 
   b) <paragraph> 
   c) <text> 
   d) <par> 

9. What is the correct HTML tag for creating an unordered list? 
   a) **<ul>** 
   b) <list> 
   c) <unordered> 
   d) <bullets> 

10. How do you insert a line break in HTML without creating a new paragraph? 
    a) **<br>** 
    b) <break> 
    c) <linebreak> 
    d) <lb> 

11. Which attribute is used to set the background color of an HTML element? 
    a) **bgcolor** 
    b) background 
    c) color 
    d) bg 

12. What does the `<title>` tag define in an HTML document? 
    a) **The document's title displayed in the browser's title bar** 
    b) The main heading of the page 
    c) The author's name 
    d) The document's metadata 

13. How can you make a text bold in HTML? 
    a) **<b>Bold Text</b>** 
    b) <bold>Bold Text</bold> 
    c) <strong>Bold Text</strong> 
    d) <weight>Bold Text</weight> 

14. Which HTML tag is used to create an ordered list? 
    a) **<ol>** 
    b) <orderedlist> 
    c) <numberlist> 
    d) <1> 

15. What is the purpose of the `<base>` tag in HTML? 
    a) **To define the base URL for relative links** 
    b) To set the document's background image 
    c) To specify the document's encoding 
    d) To create a basic structure for the page 

16. How do you create an HTML form? 
    a) **<form>** 
    b) <input type="form"> 
    c) <submit> 
    d) <createform> 

17. Which attribute is used to specify the size of a text box in an HTML form? 
    a) **size** 
    b) length 
    c) width 
    d) height 

18. What does the `<meta>` tag typically contain? 
    a) **Metadata about the document** 
    b) JavaScript code 
    c) CSS styles 
    d) HTML comments 

19. How can you insert a horizontal rule in HTML? 
    a) **<hr>** 
    b) <rule> 
    c) <horizontal> 
    d) <line> 

20. Which HTML tag is used to create an italicized text? 
    a) **<i>** 
    b) <italic> 
    c) <italics> 
    d) <emphasis>

#CSS
 Here are the 20 multiple-choice questions on CSS with their respective answers: 

1. What does CSS stand for? 
   a) **Cascading Style Sheets** 
   b) Computer Style Sheets 
   c) Creative Style Sheets 
   d) Custom Style Sheets 

2. Which selector targets all the paragraph elements on a web page? 
   a) *<p>* 
   b) #paragraph 
   c) **.paragraph** 
   d) *p* 

3. How can you set the background color of an element to red using CSS? 
   a) background-color: red; 
   b) **background: red;** 
   c) color: red; 
   d) background-color: #FF0000; 

4. What is the purpose of the `!important` declaration in CSS? 
   a) To increase the specificity of a selector 
   b) **To override any other CSS rules** 
   c) To make a rule apply to all elements 
   d) To set a default value for a property 

5. Which CSS property is used to control the spacing between letters in a text? 
   a) **letter-spacing** 
   b) word-spacing 
   c) text-indent 
   d) line-height 

6. How can you center an element horizontally within its parent container? 
   a) **margin: 0 auto;** 
   b) text-align: center; 
   c) position: absolute; left: 50%; 
   d) transform: translateX(-50%); 

7. What does the `box-sizing` property do in CSS? 
   a) **Controls the size of the element's box model** 
   b) Sets the background color of the element 
   c) Adjusts the text alignment within the element 
   d) Defines the element's position in the document flow 

8. Which CSS property is used to add a border around an element? 
   a) **border** 
   b) outline 
   c) margin 
   d) padding 

9. How can you make an element occupy the full width of its parent container? 
   a) **width: 100%;** 
   b) max-width: 100%; 
   c) min-width: 100%; 
   d) width: auto; 

10. What is the default value of the `display` property for an HTML element? 
    a) **block** 
    b) inline 
    c) none 
    d) flex 

11. Which CSS property is used to control the visibility of an element? 
    a) **visibility** 
    b) display 
    c) opacity 
    d) z-index 

12. How can you create a 3-pixel solid blue border around an element? 
    a) **border: 3px solid blue;** 
    b) border-color: blue; border-width: 3px; 
    c) outline: 3px solid blue; 
    d) border-style: solid; border-color: blue; border-width: 3px; 

13. What is the purpose of the `float` property in CSS? 
    a) **To position an element on the left or right side of its container** 
    b) To create a multi-column layout 
    c) To make an element occupy the full width of its container 
    d) To control the order of elements in a document 

14. Which CSS property is used to add a drop shadow to an element? 
    a) **box-shadow** 
    b) text-shadow 
    c) element-shadow 
    d) shadow 

15. How can you make an element's text appear larger than its actual font size? 
    a) font-size: larger; 
    b) **font-size: 1.5em;** 
    c) font-size: 150%; 
    d) font-size: 16px; 

16. What is the difference between `padding` and `margin` in CSS? 
    a) **Padding adds space inside an element, while margin adds space outside.** 
    b) Margin adds space inside an element, while padding adds space outside. 
    c) Both properties have the same effect and are interchangeable. 
    d) Padding affects the element's width, while margin affects its height. 

17. Which CSS property is used to set the background image of an element? 
    a) **background-image** 
    b) background 
    c) image 
    d) background-url 

18. How can you make an element's text appear in uppercase? 
    a) **text-transform: uppercase;** 
    b) font-style: uppercase; 
    c) text-case: uppercase; 
    d) letter-case: uppercase; 

19. What is the purpose of the `position` property in CSS? 
    a) **To define the element's position relative to its normal flow** 
    b) To set the element's background color 
    c) To control the element's visibility 
    d) To adjust the element's font size 

20. Which CSS property is used to round the corners of an element's border? 
    a) **border-radius** 
    b) corner-radius 
    c) round-corners 
    d) curve-corners

#JS
 Here are the 20 multiple-choice questions on JavaScript with their respective answers: 

1. What is the correct way to declare a variable in JavaScript? 
   a) **var myVariable;** 
   b) let myVariable; 
   c) const myVariable; 
   d) All of the above 

2. Which keyword is used to define a function in JavaScript? 
   a) **function** 
   b) def 
   c) create 
   d) None of the above 

3. How can you access the length of an array in JavaScript? 
   a) **array.length** 
   b) array.size 
   c) array.count 
   d) array.getLength() 

4. What is the purpose of the `this` keyword in JavaScript? 
   a) **To refer to the current object or function** 
   b) To declare a global variable 
   c) To access the parent object 
   d) To define a new object 

5. Which method is used to add a new element to the beginning of an array in JavaScript? 
   a) **unshift()** 
   b) shift() 
   c) push() 
   d) pop() 

6. How can you convert a string to an integer in JavaScript? 
   a) **parseInt()** 
   b) parseFloat() 
   c) toInt() 
   d) toInteger() 

7. What is the output of the following code: `console.log(typeof null);` 
   a) "object" 
   b) **"null"** 
   c) "undefined" 
   d) "number" 

8. Which loop is used to iterate over the properties of an object in JavaScript? 
   a) **for...in** 
   b) for...of 
   c) while 
   d) do...while 

9. How can you check if a variable is of type "number" in JavaScript? 
   a) **typeof variable === "number"** 
   b) variable instanceof Number 
   c) isNaN(variable) 
   d) Number.isNumber(variable) 

10. What is the result of the expression `true && false || true`? 
    a) **true** 
    b) false 
    c) undefined 
    d) NaN 

11. Which method is used to remove the last element from an array in JavaScript? 
    a) **pop()** 
    b) shift() 
    c) unshift() 
    d) splice() 

12. How can you create an empty array in JavaScript? 
    a) **var arr = [];** 
    b) var arr = {}; 
    c) var arr = ""; 
    d) var arr = null; 

13. What is the purpose of the `try...catch` block in JavaScript? 
    a) **To handle exceptions and errors** 
    b) To define a new function 
    c) To create a loop 
    d) To declare a variable 

14. Which operator is used to concatenate two strings in JavaScript? 
    a) **+** 
    b) += 
    c) ++ 
    d) = 

15. How can you access the value of an object's property using dot notation? 
    a) **object.property** 
    b) object["property"] 
    c) Both a and b 
    d) None of the above 

16. What is the output of the following code: `console.log("Hello" + 2);` 
    a) **"Hello2"** 
    b) "Hello" 
    c) "HelloNaN" 
    d) "Hello2" 

17. Which method is used to reverse the order of elements in an array in JavaScript? 
    a) **reverse()** 
    b) sort() 
    c) shift() 
    d) unshift() 

18. How can you check if a value is truthy in JavaScript? 
    a) **if (value)** 
    b) if (value === true) 
    c) if (value.truthy) 
    d) if (value.truth) 

19. What is the purpose of the `typeof` operator in JavaScript? 
    a) **To check the type of a variable** 
    b) To convert a value to a specific type 
    c) To define a new variable 
    d) To access an object's property 

20. Which method is used to find the index of a specific value in an array in JavaScript? 
    a) **indexOf()** 
    b) findIndex() 
    c) includes() 
    d) None of the above
